anthropic>=0.39.0
httpx>=0.27.0
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.0.0
websockets>=13.0
playwright>=1.40.0
pyyaml>=6.0

# Optional — richer per-platform backends for hermes.py (the reach layer).
# Without it, reach still works: routing falls back to a built-in host table
# and every URL is read through Jina Reader.
# agent-reach>=1.5.0
