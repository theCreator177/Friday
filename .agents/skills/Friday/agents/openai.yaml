interface:
  display_name: "Friday"
  short_description: "Repo-specific patterns and workflows for Friday"
  default_prompt: "Use the Friday repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true